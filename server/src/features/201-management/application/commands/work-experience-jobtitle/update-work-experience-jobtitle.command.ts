/**
 * Command for updating a work experience job title
 * Application layer command - simple type definition without validation
 */
export interface UpdateWorkExperienceJobTitleCommand {
  desc1: string;
}

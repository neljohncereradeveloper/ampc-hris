/**
 * Command for creating a work experience job title
 * Application layer command - simple type definition without validation
 */
export interface CreateWorkExperienceJobTitleCommand {
  desc1: string;
}

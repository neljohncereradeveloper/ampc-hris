/**
 * Command for creating a work experience company
 * Application layer command - simple type definition without validation
 */
export interface CreateWorkExperienceCompanyCommand {
  desc1: string;
}

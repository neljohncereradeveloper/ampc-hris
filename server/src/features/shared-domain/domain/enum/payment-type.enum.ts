/**
 * Domain enum representing employee payment types
 * These are part of the business domain's payment model
 */
export enum PaymentTypeEnum {
  DAILY = 'daily',
  MONTHLY = 'monthly',
  HOURLY = 'hourly',
}


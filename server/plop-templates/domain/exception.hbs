import { HTTP_STATUS } from '@/core/domain/constants';
import { DomainException } from '@/core/domain/exceptions/domain.exception';

/**
 * Thrown when {{camel model}} data violates domain invariants or business rules
 */
export class {{pascal model}}BusinessException extends DomainException {
  constructor(message: string, status_code: number = HTTP_STATUS.BAD_REQUEST) {
    super(message, '{{constant model}}_BUSINESS_EXCEPTION', status_code);
    this.name = '{{pascal model}}BusinessException';
  }
}
